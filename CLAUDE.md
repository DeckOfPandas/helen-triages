# Claude Code Rules for Jekyll

## Environment Constraints
- OS: Ubuntu/Debian inside Windows Subsystem for Linux (WSL).
- Environment: Local Jekyll development.
- Target Directory: ONLY operate within this folder. Do not traverse to `../` or `/mnt/c/`.

## Critical Restrictions
- NEVER run `rm -rf` except in tmp/ without explicit human confirmation.
- Do not attempt to run `rm -rf` in the middle of a long job EXCEPT in folder "tmp/" within this working directory you were launched in. Complete the job first, then tidy up afterwards, to reduce interruptions.
- NEVER alter system configurations, `.bashrc`, `.bash_profile`, `.bash_aliases`, or install global apt packages.
- NEVER access or create `.env` files containing deployment API keys.
- NEVER run commands using the `--dangerously-skip-permissions` flag.
- **NEVER change file permissions without asking me first, every single time.** No `chmod`, no `chown`, no `sudo`. Ask, name the exact files, and wait -- a blanket "yes" to one chmod is not a standing permission for the next one. `.claude/settings.json` denies these three commands outright, but as with the `/tmp` rule above, Bash can't be perfectly restricted by permission patterns, so this line is the actual backstop. The one time this came up (2026-08-17) the fix was legitimate -- `.gh-runtime/bin/gh` and `.node-runtime/node/bin/node` had been extracted without their execute bit, which is why the JS test suite had silently not been running -- and it was still Helen's call to make, not mine.
- NEVER read, write or execute above the folder you're in.
- NEVER read, write or execute in `~` (my home directory) or anywhere outside this project folder, for any reason -- if Claude Code config/settings storage is ever needed, it belongs in this project's own `.claude/` folder, never `~/.claude/`.
- NEVER read, write or execute using the system `/tmp` directory (an absolute path starting `/tmp`). Use this project's own `tmp/` folder for all scratch and temporary files. Settings-level deny rules block this for the Read/Write/Edit tools, but Bash can't be perfectly path-restricted by permission patterns -- this rule is the actual backstop.
- NEVER use `$CLAUDE_JOB_DIR` or any path under `/home/helen/.claude/jobs/` as a scratch location, even though background-job tooling suggests it by default. All scratch/temporary files -- including any helper script written to a file specifically to avoid a shell-quoting/brace-expansion warning -- belong in this project's own `tmp/` folder, always.

## Build Commands
- Dev Server: `jekyll-local`, deploying to localhost:4001
- Prod Server: `jekyll-prod`, deploying to localhost:4002

## Git workflow
- If we're starting new work, and we're currently on main, please check out a new branch.
- **Update `main` WITHOUT checking it out: `git fetch origin main:main`.** This fast-forwards the local `main` ref while you stay on your working branch, so you are never standing on `main` and can never accidentally write to it. It refuses rather than mangles anything if the update is not a clean fast-forward, which is the signal that someone has committed locally to `main` and it needs a human. This replaced `git checkout main && git pull origin main` on 2026-08-20, after a commit landed on `main` in the gap that pattern opens.
  - **IN A WORKTREE IT WILL USUALLY REFUSE, AND THAT IS NOT A PROBLEM TO SOLVE.** `fatal: refusing to fetch into branch 'refs/heads/main' checked out at '/home/helen/projects/helen-triages'` — git will not fetch into a branch that is checked out anywhere in the worktree set, and the primary clone normally sits on `main`. **Use plain `git fetch origin` instead.** It updates `origin/main`, which is all that is actually needed to merge or rebase a branch against it, and it touches no checked-out branch. Then `git merge origin/main`. Measured 2026-09-05 from `.claude/worktrees/opus-design-backlog`.
  - **NEVER "fix" that refusal by checking `main` out.** The entire point of the bullet above is that Claude never stands on `main`, and `git fetch origin` keeps that property perfectly. The refusal is git protecting the same invariant this rule exists for, not an obstacle to route around.
- Commit freely without asking.
- **NEVER commit or merge directly onto `main`, for any reason, even a one-line fix or an untangle/cleanup task.** **A hook now enforces this** -- `.claude/hooks/guard-main-branch.py`, wired as a PreToolUse hook on Bash in `.claude/settings.json`, refuses `git commit` and `git merge` whenever the target repo is on `main`. It reads any leading `cd <path>` out of the command so it asks about the RIGHT repo (the nested drafts repos are edited through `cd _food_drafts && git ...`), and it strips quoted spans and heredoc bodies so a commit message may still discuss these commands. Everything else on `main` is untouched: reading, fetching, branching, checking out, pushing an existing commit. Added 2026-08-20, the day the written rule below was read and broken anyway.
- **THE AGREED WORKFLOW, settled 2026-08-20, step 1 widened 2026-09-07 and split in two on 2026-09-08. Four steps, and only one is Helen's.**
  1. Claude works on a branch, then **pushes it and opens the PR with no ask at all** (`gh pr create`, or the REST API from a worktree, which has no `gh`). **Widened twice on 2026-09-07, in two steps, and the second is the bigger one.** First: Claude could push but never open the PR, and Helen handed the PR step over as pure manual overhead, folding the two asks into one. Then, hours later, she removed the ask entirely: *"push no longer needs my say so. I had this rule because multiple Claudes were trampling each other and it's easier to fix that locally, but I now get Claudes to run Claudes and everything is less chaotic!"* **The confirmation was never about the risk of pushing** -- it was a lock against parallel sessions fighting over one checkout, and worktrees plus an orchestrating Claude solved that at the root, so the lock was cost with nothing behind it. Since 2026-09-09 this runs on `AGENT_GH_TOKEN` in all three repos -- see step 1a and the GitHub Issues and Pull Requests section below for what is still off-limits.
  1a. **THE DEVCONTAINER CAN PUSH AND OPEN PRs NOW, UNDER A SEPARATE IDENTITY, OVER HTTPS -- NOT `origin`.** Settled 2026-09-08, superseding the same day's earlier ruling that it couldn't. The original blockers were real (SSH dies on `Host key verification failed` with no host key in the image; the fine-grained PAT deliberately carries no `Contents` scope) and neither got fixed -- instead Helen created a second GitHub account, `DeckOfPandas-agentic`, invited it as a collaborator on all three repos, and gave it its own classic `repo`-scoped PAT, stored as `AGENT_GH_TOKEN` in `.claude/settings.local.json` (alongside `GH_TOKEN`, until that was deleted on 2026-09-09 -- see below) and passed into the container by `run.sh`. Her reasoning: *"I realised today that long-running sessions without supervision mean I come back to a tangle if they can't push without me, and I risk losing work."* Verified by measurement 2026-09-08, not assumed: a real push from inside a real container succeeded, and a real PR opened this way confirmed `"author":{"login":"DeckOfPandas-agentic"}` via the API, not just a 200.
      - **`origin` is still SSH and still broken in the container -- don't use it for push.** **AND THE OLD ANSWER IS RETIRED, 2026-09-10 -- never build a URL with the token in it again.** Building the URL by hand with the token embedded (`https://DeckOfPandas-agentic:${AGENT_GH_TOKEN}@github.com/...`) worked, but a CLONE stores that URL -- token included -- as the repo's `origin` remote in `.git/config`, and every later command that surfaces a remote URL (`git remote -v`, `git remote show`, `git config -l`, `cat .git/config`, some git error messages) prints it in plain text. Measured 2026-09-10: a routine `git remote -v`, run for the unrelated and legitimate reason MANUAL §2.1 gives, printed the token straight into a transcript. **The fix is `scripts/git-credential-agent-token.sh`**, a git credential helper that reads `AGENT_GH_TOKEN` from the environment at the moment git asks for it and hands it to git on a pipe, so the token is never in a URL, never in any `.git/config`, and never in a string anything can print. **You never call it yourself. The three git wrappers pass it to git per invocation, with `-c`, against a PLAIN url:** `sh scripts/git-push-agent.sh <branch>:<branch> [repo]`, `sh scripts/git-clone-agent.sh <repo> [dir]`, `sh scripts/git-fetch-agent.sh <dir> <repo> [ref]`. Per invocation and NOT written into any repo's config, deliberately: `/workspace/.git/config` is shared by the primary checkout and every worktree, git runs a configured helper from each worktree's own top level, and a helper path that exists only on one branch fails on every other one -- measured 2026-09-10, `sh: 0: cannot open scripts/git-credential-agent-token.sh: No such file` on every push from an unrelated worktree, silently tolerated by git because the token was still in the URL, and a hard failure the moment it wasn't. `.claude/hooks/guard-token-expansion.py` refuses the embedded-URL shape outright, in a command or in a script file it runs -- see its own bullet below. PR creation uses the same account, over the API rather than git: `sh scripts/gh-agent.sh pr create ...` (the wrapper, since 2026-09-10 -- see the permissions section below for why the token's name no longer belongs at a call site).
      - **Commit AUTHORSHIP does not change.** This was a deliberate, separate decision from Helen, 2026-09-08: commits stay authored as her with the usual `Co-Authored-By: Claude` trailer -- only which account talks to GitHub (pushing the ref, opening the PR) changes. Do not set `user.name`/`user.email` to the bot anywhere.
      - **Never delete a remote branch with `AGENT_GH_TOKEN` via git push --delete casually assuming it needs a fallback** -- unlike the old fine-grained PAT, this classic token's `Contents` scope covers ref deletion, so `sh scripts/gh-agent.sh pr close --delete-branch ...` works directly. Verified 2026-09-08 on a throwaway PR.
      - **`GH_TOKEN` IS GONE. `AGENT_GH_TOKEN` IS THE ONLY CREDENTIAL, EVERYWHERE, FOR EVERYTHING.** Helen deleted `GH_TOKEN` on GitHub on 2026-09-09 and removed the read from `run.sh`; this file, `MANUAL.md`, `scripts/ingest_inbox.py`, `devcontainer.json` and the devcontainer README were updated the same day to match. It had been reduced to a strict subset of what `AGENT_GH_TOKEN` already did -- **two credentials where one was enough, and the smaller one was the one that kept generating rules about what it could not reach.** Anything that used to say `GH_TOKEN` as a SECRET now means `AGENT_GH_TOKEN`; `GH_TOKEN` survives only as **the variable name `gh` itself reads** -- an assignment, not a second token. That assignment now lives inside `scripts/gh-agent.sh` rather than at every call site (2026-09-10).
      - **AND THE PRIVATE-REPO PR RESTRICTION WENT WITH IT.** Opening a PR on `helen-triages-food-private` or `helen-triages-cocktails-private` used to 422 `not all refs are readable`, because that needs `Contents` and Helen's fine-grained token deliberately had none. `AGENT_GH_TOKEN` is classic `repo`-scoped and has it -- measured 2026-09-08, PR #25 opened on `helen-triages-food-private` and closed unmerged. **So step 1's "open the PR yourself, no ask" now holds in all three repos**, and the old "say so and let Helen open it" fallback is dead. Merging is still hers, in all three, always.
  2. **Helen reviews the PR and merges it. That is all she does.** No local checkout, no pull, nothing else. Claude opening a PR is not, and never becomes, authorization to merge it.
  3. Claude runs `git fetch origin main:main` to fast-forward local `main` without leaving its branch (or `git fetch origin` from a worktree, where the first form refuses — see the caveat above; or the plain HTTPS URL with no credentials, `git fetch https://github.com/DeckOfPandas/helen-triages.git main:main`, from inside the devcontainer, where a public repo needs no auth to read), deletes the merged branch local and remote, and branches afresh for the next piece. **Since 2026-09-11 the one form that works everywhere and never prompts is `sh scripts/git-fetch-main.sh`** (updates `origin/main` only, touches no branch), then `git merge origin/main`.
  4. Repeat. Claude is never on `main`, so the hook above should never actually fire.

  Helen is welcome to pull locally if she wants to -- it is a fast-forward either way and cannot conflict with step 3. The point is that nothing REQUIRES her to, because the old pattern (she merges, checks out `main`, pulls, and Claude carries on assuming the old branch) is what opened the gap. `main` only ever moves via a merged PR, never a direct local write.
- **Every PR's base is `main`. Never stack a PR on another PR's branch, and read a PR's `state` before pushing to any branch you did not open this session.** Both cost the live site on 2026-09-11. GitHub moves a stacked PR onto `main` only when its base BRANCH is deleted, not when the base's PR merges, so #970 merged into #968's branch 24 seconds after #968 merged and #961–#967 never deployed. And a merged PR's branch still accepts a push: #960's first fix went to `feat/related-drinks` after #955 had merged, and reached nothing. If a second piece needs the first's tooling, say "merge #A first" and rebase onto `main` once it has; `sh scripts/gh-agent.sh pr view N --json state` answers the second in one call. DECISIONS §11 has the story.
- **Say when you have merged, if it is easy.** Not for correctness -- step 3 discovers it anyway -- but a merge changes which branch is current, and being told beats finding out. Equally: if Claude has been told nothing, it should check rather than assume.
- **This applies to EVERY repo in the working tree, not just `helen-triages`.** The nested private drafts repos (`_food_drafts/`, `_cocktail_drafts/`) are separate repos with their own `main`, and the same rule governs them: branch, never commit to `main`. Got this wrong on 2026-08-17 -- four commits went straight onto `_cocktail_drafts`' `main` because the rule read as if there were only one repo. They were moved to a branch and `main` reset to `origin/main`; nothing had been pushed.
- **NEVER run `git reset --hard` (or `git checkout --` / `git restore` over a dirty tree, or `git clean -fd`) without asking me first, every single time.** **A hook now enforces this** -- `.claude/hooks/guard-destructive-git.py`, wired as a PreToolUse hook on Bash in `.claude/settings.json`, refuses these commands whenever `git status` is not clean, and names what would be lost. It deliberately allows them on a CLEAN tree (a no-op is not dangerous, and a guard that fires on harmless invocations teaches you to route around it) and ignores quoted mentions and heredoc bodies, so a commit message may still discuss them. **It caught `git checkout <path>` only from 2026-08-19 onwards** -- the first version patterned the `git checkout -- <paths>` form alone, and the bare-path form walked straight past it and destroyed a file's uncommitted work the same day. Telling a path from a branch cannot be done by pattern (`git checkout main` is harmless, `git checkout feat/thing` looks exactly like a path), so it now asks the filesystem whether the argument names a real file. Added 2026-08-19 because this written rule was read and then broken the same day: an agent ran `git checkout -- <two files> 2>/dev/null || true` to undo its own edit and discarded work with it. The lesson generalises and is the reason the hook exists rather than a firmer sentence: a rule I read and break needs enforcement, not rewording -- the same conclusion this repo already reached about `meta.awaiting_fix` and `meta.proofread`. These discard uncommitted work with no undo and no reflog entry to recover it from. Ask, name what will be lost, and wait. Got this wrong on 2026-08-18: I ran `git reset --hard origin/main` to move a commit off `main` and silently destroyed a half-finished handover edit sitting uncommitted in the same tree -- the commit was safe, the uncommitted work was not. **Check `git status` first and say what it shows.** If something needs discarding and I am not around, `git stash -u` instead: it is reversible.
- **Check `git branch --show-current` immediately before every `git commit`, IN A SEPARATE TOOL CALL.** Not at the start of the task, and not in the same shell command as the commit. This is how commits landed on `main` twice on 2026-08-18 -- and then again on 2026-08-20 by an agent that *did* run the check, in this shape:

      git branch --show-current      <- the check
      git add ...
      git commit -F ...              <- already done by the time it printed

  The check ran and printed `main` in the output of the call that had already committed. **A check that cannot stop the thing it is checking is a narration, not a check.** Run it, read the answer, then decide. The hook above is the backstop for when this is forgotten again, but the habit is what should make the hook redundant.

  **It earned its keep for a second reason on 2026-09-07, and it is not the one it was written for.** `/workspace` is a bind mount of Helen's checkout, so another session — or Helen herself — switching branch moves the ground under a running Claude with no signal at all. In one session the branch went `main` → `docs/bash-friction-followups` → `chore/devcontainer-per-worktree-bundle-cache` → `docs/multiline-arg-friction`, none of them that session's, and an uncommitted `DECISIONS.md` edit rode along into somebody else's branch. Nothing was lost, because the pre-commit check caught it. **So the question the check answers is not only "am I on `main`" but "am I still where I left off"** — and the answer can be no even when nothing you did changed it. Helen's fix, 2026-09-08: *"I'll run Claudes in worktrees going forwards."* Until every session is in one, re-check after any long gap, not just before a commit.
- **Pushing needs no ask, in any repo in the tree, since 2026-09-07 -- wherever pushing works at all. In the devcontainer it does not, and step 1a above is what happens instead.** Her words are in step 1 of the workflow above, and so is the reason the old rule existed. This paragraph is what it replaced, kept because the PRIVATE repos got there first and the argument for them is still the argument. **Pushing is not merging**: `main` only ever moves via a PR Helen merges, in every repo, and the hook still refuses a commit or a merge onto it. Until 2026-09-07 the rule read *"Never `git push` without my explicit confirmation first"* -- with an exception for the two PRIVATE drafts repos, where pushing needed no ask, `main` or a branch. Stated 2026-08-29: *"Pushing to main in the private repos is fine."*; widened to branches 2026-09-05, when the publication workflow made it routine. Nothing in `_food_drafts/` or `_cocktail_drafts/` triggers a build, so a push there has none of the every-merge-is-a-deploy cost that shapes the workflow in this repo, and a commit of mine sitting unpushed on one disk exists in exactly one place, which is the real risk. **A promotion batch depends on this** -- `model_instructions/PUBLISHING_A_DRINK.md` has one working copy, always pushed. **This does NOT relax the rule against COMMITTING or MERGING onto `main`** -- that still holds in every repo in the tree, the hook still enforces it, and Claude's own work in those repos still goes on a branch. `helen-triages` used to be the exception to all of this -- *"never push it without explicit confirmation"* -- and stopped being one on 2026-09-07; the three repos now behave the same way for pushing, and identically for merging, which is the simplification the 2026-09-07 ruling actually bought.
- **ALWAYS tag issues in commit messages.** If a commit resolves a GitHub Issue, add a `Fixes #N` (or `Closes #N`) trailer for every issue it resolves; if it only advances or touches one, cite it in the body (`Towards #N`, `See #N`). GitHub auto-closes on `Fixes`/`Closes` once the commit reaches `main`. Do this at commit time -- while nothing is pushed a message can still be rewritten, but after a push it is fixed. When summarising work, list the issue numbers involved.
- **A bare `#N` only resolves within its OWN repository.** The nested private drafts repos have their own (empty) trackers, so `Towards #314` in a commit there links to nothing. Cross-repo references need the full form: `Towards DeckOfPandas/helen-triages#314`.

## Proofreading -- Helen is the last judgement before a recipe publishes
- **If you edit a recipe file, set `meta.proofread: false` in the SAME commit.** Issue #367. Helen proofreads every recipe by hand before it goes live; if Claude changes the file afterwards, her proofread no longer covers what is in it. This is not a formality -- `meta.awaiting_fix` and `meta.proofread` are what the publish gate (#331) and the index badges hang off.
- **THE GATE IS TWO FLAGS AND IT FAILS CLOSED: a recipe or drink publishes only if it says `awaiting_fix: false` AND `proofread: true`.** Both, on both sites, since 2026-09-02 (#667, Helen's ruling D3 -- proofread "is the very last touch that I, the human, make to the file"). Not "true hides it" -- an explicit pass on both is the only thing that lets a page through. Either flag missing, the old hyphenated `awaiting-fix`, or a quoted string: all held back, and all break a test, which stops the build (#369). Never write the hyphenated form: Liquid parses `page.meta.awaiting-fix` as SUBTRACTION, so a template asking whether a page is flagged gets arithmetic and publishes it. The mechanism is `_plugins/publish_gate.rb` (called `hide_awaiting_fix.rb` until the second flag joined), and its build-log line names which flag held each page back.
- **It applies to any change to the file, including ones that look safe.** On 2026-08-18 twelve proofread recipes were edited in one commit (a wording change to eight of them, a note added to two, an ingredient group renamed) and not one flag was touched. Nothing noticed, because nothing was looking.
- `tests/test_front_matter.py::test_agent_edited_recipes_are_not_marked_proofread` enforces it by reading git history: if a recipe's newest commit carries a `Co-Authored-By: Claude` trailer, the file must say `proofread: false`.
- **The exception, and it is Helen's to grant, not yours.** If she has reviewed the change herself -- line by line, as she did for the 45 second-person edits on 2026-08-18 -- then she was the last judgement even though an agent's commit wrote the bytes. In that case move `BASELINE_COMMIT` in that test forward and say so in the commit message. Never move it to make a red test go green. Drinks have the same constant, `COCKTAIL_BASELINE_COMMIT` in `tests/test_cocktails.py`, and it moves the same way: in a commit of its own, quoting her grant, and proved afterwards by running the test with the old value and watching it name the drinks (2026-09-10, five moves in one day -- the constant's own comment is the history). **The cheapest shape, found the same day: if she reads the rendered pages ON THE BRANCH before it merges and says so, flip the flags back in a second commit and move the baseline in a third, and nothing ever leaves the live site.**

## GitHub Issues and Pull Requests -- read/write, never merge, never repo settings
- **Changed 2026-08-17.** This used to say read-only, always. Claude may READ, RAISE, CLOSE, REOPEN, COMMENT ON, LABEL and ASSIGN issues on `DeckOfPandas/helen-triages`, `helen-triages-food-private` and `helen-triages-cocktails-private`. Those three repositories and no others.
- **Widened 2026-09-07.** Claude may also OPEN pull requests (`gh pr create`, or the REST API from a worktree), folded into the single push-and-open-PR step in the Git workflow section above. Helen was fine trading this bit of autonomy for not having to click through GitHub's PR form herself every time.
- **ONE CREDENTIAL SINCE 2026-09-09: `AGENT_GH_TOKEN`, and it works on all three repos.** Helen deleted `GH_TOKEN` that day. Everything above now runs on `DeckOfPandas-agentic`'s classic `repo`-scoped PAT (Git workflow step 1a), which was already the credential for the devcontainer and for the private repos. **The old public-only PR restriction is GONE with it** -- opening a PR on a private repo used to 422 `not all refs are readable` because the fine-grained token had no `Contents`; measured 2026-09-08, `AGENT_GH_TOKEN` opened PR #25 on `helen-triages-food-private` fine. Branch deletion works directly too (`sh scripts/gh-agent.sh pr close --delete-branch ...`), where the old token needed `git push --delete` as a fallback. `DECISIONS.md` §11 holds the measured scope tables for the retired token; don't re-derive them, they describe a credential that no longer exists.
- **MERGING IS NOT PART OF THIS, AND SINCE 2026-09-09 ONLY THE RULE STOPS IT.** Claude opening a PR never implies merging it, closing it as merged, or approving it -- that stays entirely Helen's, in all three repos, per the Git workflow section above. **What changed is the enforcement, not the rule.** The fine-grained token could not merge; a classic `repo`-scoped token held by a collaborator CAN. So the sentence this section used to end on -- *"the token is scoped so the rest is impossible"* -- is no longer true of merging, and the honest statement is: **merging is now prevented by nothing but this line, so treat it as absolute.** Repo settings, secrets, Actions, webhooks and collaborators do still need admin rights the agent account does not have.
- **Never merge a pull request, never change repository settings, secrets, Actions, webhooks or collaborators, and never act on any other repository.** Pushing code follows the Git workflow section above and needs no ask in any of the three repos -- and **pushing a branch has never been, and does not become, a way to move `main`**.
- **DON'T PROBE A TOKEN'S SCOPE -- USE IT AND READ THE STATUS CODE.** A 401 or 403 is the answer, and it never renders the value. Two ways this has gone wrong: **`GET /user/repos` DOES NOT REVEAL A FINE-GRAINED TOKEN'S SCOPE** -- it lists what the ACCOUNT owns regardless of which repos the token selected, and reading it as the token's reach produced a confident, wrong report; and a shell probe for whether the variable is set is refused outright by `guard-token-expansion.py`, see the two bullets below. If a token is ever regenerated, re-measure with real permission-requiring calls against a specific repo -- **a widened token is invisible until something unexpected succeeds.**
- **That is the entire permission.** Do not treat a 403 as a problem to route around.
- **Never attempt to broaden access beyond what Helen has explicitly authorized above.** No `gh auth login`, no requesting a wider token, no adding scopes or repositories on your own initiative, no second credential. If something genuinely needs a permission not covered here, say so and stop -- I will do it by hand. (This used to end "no suggesting a classic token"; a classic token is now the only one there is, and it was Helen's to create, which is exactly the shape this rule describes.)
- **Never print, echo, log or commit the token**, and never write it into a file in this repo. Read it from the environment at the point of use. **NEVER PUT A SECRET'S NAME IN AN `echo` OR `printf` AT ALL -- there is no safe form and no exception to remember.** Not `${TOK:-x}` (which prints the whole token when it IS set, and did, on 2026-09-06), and **not `${TOK:+set}` or `${#TOK}` either, which cannot leak and are still refused.** **DON'T PROBE FOR A CREDENTIAL -- USE IT AND READ THE RESULT.** A 401 or a 403 answers "is it there and does it work" exactly, which a probe never did; in Python, `os.environ["AGENT_GH_TOKEN"]` raises a clear `KeyError` when it is missing. Using `${TOK}` where it is CONSUMED rather than printed is fine and always was: a credential helper, a URL passed to `git push`.
- **FOR `gh`, USE THE WRAPPER AND DON'T NAME THE TOKEN AT ALL: `sh scripts/gh-agent.sh <args>`.** Added 2026-09-10 at Helen's request. `GH_TOKEN="$AGENT_GH_TOKEN" gh ...` was the documented shape and is genuinely safe -- an environment assignment hands the value to `gh` and prints nothing -- but she rejected such a call on sight and asked *"is there anything we can do to stop this?"*, which is **the same argument that retired `${TOK:+set}` on 2026-09-09: from the outside a command with a secret's name in it is indistinguishable from a leak until you have run the rule in your head, and she should not have to clear a call to find out it was fine.** The wrapper puts the name in one file that can be read once, so every call site is plain: `sh scripts/gh-agent.sh issue list --repo DeckOfPandas/helen-triages`. Run it via `sh`, so it needs no execute bit and so no chmod. It deliberately does not check the credential is present -- use it and read `gh`'s own 401, per the bullet above.
- **WIDENED 2026-09-09, AND THE REASON IS NOT THAT ANYTHING LEAKED.** The rule above used to name `${GH_TOKEN:+set}` as "the only safe probe", and the hook allowed it. A session then ran `echo "${GH_TOKEN:+GH_TOKEN set}"` -- genuinely safe, correctly allowed -- and Helen rejected the call by hand anyway, because **from the outside it is indistinguishable from a leak until you have run the hook's regex in your head**: *"I shouldn't have to reject the call!! I'm only human!"* A guard whose exceptions a human has to verify has moved the work, not removed it. And nothing was given up by closing it, which is what made it obvious: *"I can't imagine why we wouldn't do that having thought of it."* **The rule is now checkable at a glance -- an `echo` never mentions a secret.**
- **A hook enforces that** -- `.claude/hooks/guard-token-expansion.py`, wired as a PreToolUse hook on Bash in `.claude/settings.json`. It refuses the four default-value expansions (`${TOK:-x}`, `${TOK:=x}`, `${TOK-x}`, `${TOK=x}`, all of which evaluate to the variable's own value when it is set) anywhere in a command, and **any mention of a secret by an `echo`/`printf`, in any form**, including through a pipeline. It allows `${TOK}` where it is CONSUMED rather than printed. **It strips single-quoted spans but NOT double-quoted ones**, unlike `guard-sed.py`, because the shell expands `$VAR` inside double quotes: `echo "$AGENT_GH_TOKEN"` is a real leak wearing quotes -- so prose about these forms belongs in single quotes. Added 2026-09-08 after the written rule was read and broken a third time; widened 2026-09-09 as above. **A rule I read and break needs enforcement, not rewording** -- the same conclusion this repo reached about `git commit` on `main` and about `sed`.
- **WIDENED AGAIN 2026-09-10, FOR A LEAK THAT WASN'T AN ECHO AT ALL.** CLAUDE.md's own documented pattern for the private repos -- embedding the token in a clone/push URL by hand -- was followed exactly as written, and git stored the resulting URL in `.git/config`. A routine `git remote -v` then printed it. Two more checks now run, on every command AND on the content of any script FILE that command runs (`sh tmp/thing.sh`, `python3 tmp/thing.py`, ...) -- because the sanctioned workaround for every OTHER guard in this directory is "put it in a file and run the file", and a check that only reads the command line is blind to exactly the commands most likely to carry this pattern:
  - **A secret embedded in a URL's userinfo** (`https://user:${AGENT_GH_TOKEN}@host`) is refused outright, regardless of quoting -- unlike the echo case, quoting genuinely doesn't make this shape safe, since the pattern is what gets stored on disk, not just what happens to expand this one time. Use `scripts/git-credential-agent-token.sh` instead (see step 1a above).
  - **A real GitHub-token-shaped string** (`ghp_...`, `gho_...`, `github_pat_...`, and siblings) is refused anywhere it appears, because the token above is now sitting in a session transcript and a future command pasting it back in from there is exactly as dangerous as one that expands it fresh. If you're reading this because a token you have access to has appeared in a transcript, assume it needs rotating -- that's Helen's call, not something a hook can undo.
  - A script's own `printf`/`echo` of the token is deliberately NOT flagged by the file-content scan -- `scripts/git-credential-agent-token.sh` genuinely must write the token to its stdout to answer git's credential protocol, and that's a pipe to git, not a transcript.
- **CLOSE AN ISSUE WITH A COMMIT TRAILER, NOT THE API, WHENEVER A TRAILER CAN DO IT. This is a standing preference, stated 2026-08-29:** *"Close #540 (via commit message if possible -- this is always my preference)."* A `Fixes #N` / `Closes #N` trailer ties the closure to the commit that earned it, so the issue's history shows what actually resolved it instead of a bare "closed" with no link to the code. Reach for the API only where a trailer genuinely cannot work -- the clear case being work done in one of the nested private drafts repos, since a cross-repo trailer from a private repo does not close or even cross-reference a public issue (see the trap on this below).
- Closing an issue is still a real action on a shared, outward-facing thing: say which issues you are about to close and why before doing it, unless I have just told you to close them. That applies to a trailer too -- name the issues a commit will close before pushing, while the message can still be rewritten.

## The claude.ai Project is a copy, and only Helen can refresh it
- `model_instructions/INGEST_ONE_RECIPE.md`, `INGEST_ONE_COCKTAIL.md` and the §2 block of `CLAUDE_WEB_INGEST.md` are uploaded by hand into a claude.ai Project (that file's §1). **If a commit of yours touches any of them, say so in your summary, in so many words: "the Project needs re-uploading."** `test_the_web_project_holds_the_current_documents` goes red locally until she does it and moves the `Uploaded to the Project as of:` line; **never move that line yourself** -- it records an upload, and you cannot upload.

## Normal workflow
- If you need a scratch temporary folder, create one in this project folder and add it to .gitignore
- Don't delete handover or jobs list documents.
- You don't need to ask permission to cd into folders at or below /home/helen/projects/helen-triages/food.
- You don't need to ask permission to read or write to tmp in /home/helen/projects/helen-triages/food.
- **Never run multi-line scripts via a Bash heredoc (`python3 - <<'PY' ... PY`, `ruby <<'RB'`, etc.).** Claude Code's permission checker statically parses a Bash command to prove it won't read outside the working directory; a heredoc's body arrives on stdin, which is opaque to that parser, so it can NEVER be allow-listed and always forces a manual prompt -- even under auto mode, even for trivial edits, even mid an otherwise-unattended job. Two working alternatives, because the fix is specifically "don't deliver the code via stdin":
  - ~~Short snippets with no embedded single quotes: `python3 -c '<code>'`~~ **WRONG, AND IT WAS WRONG FROM THE START -- CORRECTED 2026-09-10.** This bullet used to say a short single-quoted `-c` snippet is "analyzable and already matches the existing `Bash(python3 -c ' *)` allow rule". **There is no such allow rule in `.claude/settings.json`, and there never was** -- go and look. And it would not have helped: `settings.json` sets `blockReadsOutsideWorkingDirectories`, and under that block a command the shell parser cannot analyze asks Helen *whatever the allow list says*, because the checker has to prove the command reads only inside the working directory and it cannot prove that about code it cannot see. An inline program is opaque by construction. **There is no short-enough and no quote-free-enough.** Found when a 78-character single-quoted one-liner -- inside every limit this bullet and the hook set -- prompted her anyway, and she asked to either do it more safely or not be asked at all.
  - **So: EVERY python/ruby/node/perl program goes in a file, however short.** Write it to a file in `tmp/` (or the relevant repo's tmp) with the Write tool, then run it as a plain file argument, e.g. `python3 tmp/fix_thing.py`. That is statically analysable, so it is never refused -- **but it still asks Helen, on purpose.** There is no `Bash(python3 *)` allow rule and never was (this line claimed one until 2026-09-11, a day after the bullet above found the same false claim), and on 2026-09-11 she ruled there will be none for `tmp/`: *"keep asking me please."* The checker sees a script's name, not what it does. So reach first for a committed, allow-listed wrapper (listed in the allow-rules bullet below); a `tmp/` script is for the genuine one-off.
  Diagnosed 2026-09-07 after this looked like a permission-state regression but was actually just heredoc usage creeping in.
- **Never prefix a Bash command with `cd /workspace;` (or any `cd`).** The Bash tool already runs with the working directory set to the project root by default -- there is nothing to `cd` into. All it does is break every prefix-based allow rule you have: `Bash(grep *)` matches a command that STARTS with `grep`, and `cd /workspace; grep ...` starts with `cd`, so it matches nothing and always prompts, in every permission mode, no matter how good the allow-list is. Diagnosed 2026-09-07 inside the devcontainer, where a session had picked up the habit and every single command was prompting despite auto mode being on and the allow-list being fully present (it's part of the bind-mounted project directory, same as on the host).
- **Never glob a file-path argument to a read command (`grep -rl "x" dir/*.md`, `cat some/*.txt`, etc.) -- point the command at the directory and let it recurse, or list files explicitly.** Same underlying mechanism as the heredoc rule above: the permission checker statically verifies a command's file-path arguments stay inside the working directory, but a shell glob expands at runtime, so the actual list of files being read isn't known until after the check would need to run -- it can't be verified, so it always hard-asks, regardless of mode or allow-list, exactly like unanalyzable stdin. Most tools that take a glob also take a bare directory and recurse into it themselves (`grep -r "x" dir/` needs no `*.md` at all) -- that's a single static path, fully analyzable, and actually matches an allow rule. Diagnosed 2026-09-07 alongside the `cd` prefix issue, same session, same symptom.
- **Never build a multi-line commit message or PR body with `"$(cat <<'EOF' ... EOF)"`.** Third variant of the same heredoc problem, easy to miss because it LOOKS like a normal quoted argument rather than an obvious heredoc: the value only exists once the command substitution actually runs, so the checker can't verify it ahead of time and always hard-asks -- `git commit -m "$(cat <<'EOF' ...)"` and `gh pr create --body "$(cat <<'EOF' ...)"` both have this problem, no matter how good the allow-list is. Write the message/body to a file in `tmp/` with the Write tool first, then pass the file: `git commit -F tmp/commit-msg.txt`, `gh pr create --body-file tmp/pr-body.md` (plus the rest of the usual flags). Both are then a single static command with no runtime-computed content, so they're actually analyzable and can be allow-listed. Diagnosed 2026-09-07 after Helen flagged a `gh pr create` prompt as one of "a handful" of similar ones -- this exact pattern had been used for essentially every commit and PR all session.
- **One command per Bash call. Never chain with `;` or `&&` to save a round trip.** Helen, 2026-09-08: *"Please write complex commands out one at a time, so they can be statically analysed."* A chained command is checked as a whole, so `grep ... && node ...` matches neither the `Bash(grep *)` rule nor the `Bash(node *)` one and hard-asks -- the same prefix-matching mechanism as the `cd` rule above, and it fires even when every part on its own is allow-listed. Two calls cost a round trip; one chained call costs an interruption, and that lands on Helen rather than on you.
- **Anything with a pipe, a variable, a substitution or a loop goes in a script in `tmp/`.** Her rule, same day: *"if they take or emit variables, please write a script in tmp/ so you don't need to request permissions from me."* A pipeline's later stages, any `$VAR` and any `for` body are runtime-computed, so the checker cannot see what will actually run and will not allow-list it -- identical to the heredoc and glob cases. Write it with the Write tool and run the file: `sh tmp/thing.sh`, `python3 tmp/thing.py`, `ruby tmp/thing.rb`. One static path, analyzable, and with a second payoff: the script is a record of exactly what was measured, which a one-off pipeline never is. **It still prompts** (2026-09-11, the corrected bullet above): a script beats an unanalysable one-liner, which is refused outright, and a committed allow-listed wrapper beats a script.
- **`--body-file`, never `-b "..."`, for any `gh` body or comment.** Helen, 2026-09-08. The same shape as the commit-message rule above, and easy to miss because a quoted string looks innocent -- but a multi-line body full of backticks and quotes is precisely the argument the checker cannot verify, and backticks inside a double-quoted shell string are command substitution as well, so it is a correctness risk and not only a friction one. Write the body with the Write tool and pass `--body-file tmp/whatever.md`. Applies to `gh issue comment`, `gh issue create`, `gh pr create` and `gh pr edit` alike. **Except that `pr edit` itself fails on this token** (it is GraphQL and wants `read:org`, measured 2026-09-10) -- change a PR's body with `sh scripts/gh-agent.sh api -X PATCH repos/DeckOfPandas/<repo>/pulls/<N> -F body=@tmp/whatever.md`, which is REST and needs nothing the token lacks. MANUAL §11 has the detail.
- **All of these rules are one rule, and it is worth stating once.** The permission checker proves, before anything runs, that a command touches only the working directory. It can do that only for a command whose text is its whole meaning. Every rule above names a way of hiding meaning from it -- on stdin, behind a glob, behind a substitution, behind a `cd`, behind an `&&`, behind a variable -- and the cost is never a refusal, always an interruption to Helen. **When a command needs to be clever, put the cleverness in a file and run the file.**
- **A HOOK NOW ENFORCES THAT ONE RULE, SO AN UNANALYSABLE COMMAND IS REFUSED RATHER THAN PUT TO HELEN** -- `.claude/hooks/guard-unanalyzable-bash.py`, added 2026-09-10 at her request: *"can we either avoid needing to request permission, or block the command if it can't be statically analysed?"* It refuses a heredoc, `$(...)` or backticks, a leading `cd`, a pipe, `&&`/`||`/`;` chaining, and a glob in an argument. **The point is who pays.** A prompt is not a refusal -- it is an interruption, and it lands on Helen; a denial lands on the session, which can write the script and carry on. **It allows**: redirection to a static path, `2>&1` and `>/dev/null` included (a redirection names its file, so only a PIPE hides a later stage); anything inside quotes for `&&`, `||`, `;`, `|` and globs; and a bare `$VAR`, which is left to `guard-token-expansion.py` because a `$` in a regex is too common to pattern-match. **The one asymmetry to remember: `$(...)` and backticks are NOT exempt inside DOUBLE quotes**, because the shell expands them there -- so prose about substitution goes in single quotes. That was a real bug in the guard's first draft, found by breaking it on purpose.
- **And the routine calls are allow-listed, so they do not prompt either -- AND AN ALLOW RULE IS ONLY AS NARROW AS WHAT IT LETS THROUGH.** `.claude/settings.json` allows, as of 2026-09-11: the `gh` wrapper's `issue list`/`view`/`comment`/`create`/`edit`, `pr list`/`view`/`diff`/`create`/`comment`, `repo clone`; **`sh scripts/gh-read.sh <endpoint> [--jq <expr>] [--paginate]` for every REST read** (GET only, the three repos only -- never `sh scripts/gh-agent.sh api` for a read, which prompts because `api` is also the door to a merge); the three git wrappers, `git-push-agent.sh`, `git-fetch-agent.sh` and `git-clone-agent.sh`; `sh scripts/git-fetch-main.sh` then `git merge origin/main` to catch a worktree up with `main`; `git branch --show-current`; `git add -- <paths>` (the `--` is what makes it safe: every later word is a path, never `--chmod`); `python3 scripts/verify.py`; `node --test`; and `scripts/browser/`'s `install.sh`, `build.sh`, `serve.sh`, `shoot.sh` and `crop.sh`. **`pr merge` and `pr review` are in the DENY list**, in all three spellings, which is worth stating plainly: since 2026-09-09 merging has been held by the rule alone (a classic `repo`-scoped token can merge), and a blanket allow on the wrapper would have removed even the prompt. A deny rule cannot be overridden by an allow. **Before adding a rule, ask what the prompt was the last guard of.** A rule ending ` *` accepts ANY further argument, and `git fetch --upload-pack=`, `git ls-remote --upload-pack=`, `git clone --template=` and `node --import` all run a program -- so prefer an exact command, or a committed wrapper that checks its own arguments. `tests/test_agent_wrappers.py` pins which open-ended rules exist and proves each wrapper refuses what it must; the push wrapper refuses `main` on `helen-triages`, which nothing else did once pushing stopped prompting.
- **A hook now enforces the `-c` half of that** -- `.claude/hooks/guard-inline-script.py`, added 2026-09-09 after the rule above was read and broken twice in one session, both times with a multi-line `python3 -c`. Helen, the second time: *"please please please write those long lines to files rather than running them all together."* It refuses **every** inline program given to `python -c` / `ruby -e` / `node -e` / `perl -e`; any command running a FILE still passes, and so does `-c` on something that is not an interpreter (`git -c credential.helper=...`). `sh -c` and `bash -c` are deliberately not covered, because that is how a git credential helper is spelled. **The length and quoting carve-outs were removed on 2026-09-10** -- it used to allow a single-quoted one-liner under 100 characters, on the strength of an allow rule that does not exist (see the corrected bullet above), and the read block would have asked anyway. The three calibrations that threshold went through (160 -> 120 -> 100, each from a real measurement) were all hunting for a length at which an opaque command stops being opaque, and **there is no such length**. Do not reinstate the carve-out by shortening your program.
